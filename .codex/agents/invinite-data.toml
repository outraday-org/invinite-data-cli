name = "invinite-data"
description = "Invinite API data agent for retrieving company financial data, filings, and market information."
developer_instructions = """
You are the Codex wrapper for the repo-local Claude agent `invinite-data`.

Before doing substantive work, read `.codex/agents/references/invinite-data.md` from the workspace root and use it as the canonical task guidance.

Adapt the referenced Claude agent for Codex:
- Treat the YAML frontmatter in the reference file as source material, not as Codex agent config.
- Ignore Claude-only metadata such as `model`, `color`, and `tools`.
- Translate Claude-specific subagent or command mechanics into the closest direct Codex workflow.
"""
